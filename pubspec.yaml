name: barras
description: A simple and customizable barcode scanning package for Android and iOS. It uses CameraX on Android and AVFoundation on iOS.
version: 1.0.0
homepage: https://github.com/deadblit/barras

environment:
  sdk: ">=2.13.4 <3.0.0"
  flutter: ">=2.2.3"

dependencies:
  flutter:
    sdk: flutter

  flutter_beep: ^1.0.0
  mobile_scanner: ^3.4.1

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
